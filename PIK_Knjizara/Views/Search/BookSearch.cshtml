@model IEnumerable<PIK_Library.Model.Book>

@{
    ViewBag.Title = "BookSearch";
}

<h2>BookSearch</h2>

@Html.Label(expression: "book", labelText: "Book:")
@Html.TextBox(name: "book", htmlAttributes: new { @class = "form-control" }, value: null)

<h2>Books</h2>

@Html.Partial("_GetBooks", Model)

@section scripts{
    <script>

        $("#book").autocomplete({
                source: '@Url.Action(actionName: "GetAutocompleteBooks", controllerName:"Book")',
                select: function (e, ui) {
                    e.preventDefault();
                    var book = {
                        name: ui.item.label,
                        id: ui.item.value
                    };
                    $(this).val(book.name);
                    showBook(book.id);
                },
                focus: function (e, ui) {
                    e.preventDefault();
                    $(this).val(ui.item.label);
                }
            }
        );

        function showBook(id) {
            window.location.href = '@Url.Action("Index", "Book")?id=' + id;
        }

    </script>
}
